<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         requireCoverageMetadata="false"
         beStrictAboutCoverageMetadata="true"
         beStrictAboutOutputDuringTests="true"
         failOnRisky="true"
         failOnWarning="false"
         failOnDeprecation="false">
    <testsuites>
        <testsuite name="Unit Tests">
            <directory>tests/Unit</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">lib/</directory>
        </include>
        <exclude>
            <directory>lib/Migration/</directory>
        </exclude>
    </source>
    <php>
        <!--
            Hard memory cap for every PHPUnit run. The CLI php.ini on developer
            machines sets memory_limit=-1, so a runaway test has nothing to stop
            it: on 2026-09-08 one openregister test recursed inside the DI
            container and took 19 GB of RAM. The Unit suite of this app peaks at
            123 MB without coverage; 2G leaves room for coverage runs and
            still fails a leak within seconds. Raise it here, with the new
            measurement, never with -d memory_limit on the command line.
        -->
        <ini name="memory_limit" value="2G"/>
        <env name="PHPUNIT_RUN" value="1"/>
        <!--
            Boot the real Nextcloud WHEN THIS TREE SITS INSIDE AN INSTALLED ONE,
            which is exactly CI's PHPUnit job (server/apps/launchpad inside a
            server it installed with `occ maintenance:install`). Unset, that job
            ran every test against OCP stubs, so a test that needs a real
            database skipped there and could never fail. That is how #612
            shipped: the importer never set created_at, every insert failed
            with SQLSTATE 23502, and the mocked unit tests stayed green.

            Outside an installed instance (a plain clone running
            `composer check:strict`), tests/bootstrap.php refuses the full boot
            on its own `installed` check, prints one line saying so, and falls
            back to the stubs. Measured before enabling: the whole suite under
            a full boot, on Nextcloud 34 with PostgreSQL, 1690 tests, exit 0.
        -->
        <env name="PHPUNIT_USE_NC_BOOTSTRAP" value="1"/>
    </php>
</phpunit>
